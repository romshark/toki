// Code generated by github.com/romshark/datapages; DO NOT EDIT.

// Package httperr provides sentinel errors for action handlers.
// Return a sentinel directly for zero-alloc error responses,
// or wrap it with an original error using fmt.Errorf:
//
//	return httperr.BadRequest
//	return fmt.Errorf("%w: %w", httperr.BadRequest, errInvalidInput)
//
// The HTTP response body always uses the standard status text
// (e.g. "Bad Request" for 400), regardless of the error message.
//
// If an error wraps multiple sentinels (e.g. both BadRequest and NotFound),
// the first match wins. Sentinels are checked in order: BadRequest, Forbidden,
// NotFound. Wrapping multiple sentinels in a single error is a programming
// mistake and should be avoided.
package httperr

import (
	"errors"
	"net/http"
)

// Sentinel errors for HTTP status codes.
// The generated server checks for these using errors.Is
// and responds with the corresponding status code and standard status text.
var (
	BadRequest = errors.New(http.StatusText(http.StatusBadRequest)) // 400
	Forbidden  = errors.New(http.StatusText(http.StatusForbidden))  // 403
	NotFound   = errors.New(http.StatusText(http.StatusNotFound))   // 404
)

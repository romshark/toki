# todo

The CLI works ?

The Web GUI works ? MCP testing

BUT, its too much human in the loop.. 

Once we know all the right commands in the taskfile, can we write a wrapper golang thing that does it all ? think, do not code. 

THen once under control add the markdown feature, so that it can also translate markodwn. it will need to use markdown extensions.  See: https://github.com/romshark/toki/issues/13

The Webedit feature uses HTMX, and we want to use DataStar. But he will not allow this upstream. 
